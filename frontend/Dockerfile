# frontend/Dockerfile
FROM node:14
WORKDIR /app
COPY . .
RUN npm install
RUN npm run build

# Serve the React app using NGINX
FROM nginx:alpine
COPY --from=0 /app/build /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
